//Util-file for functions

//SwipableList.js
export function deleteRow(secId, rowId, rowMap) {
    /*
    rowMap[`${secId}${rowId}`].props.closeRow();
    const newData = [...this.state.listViewData];
    newData.splice(rowId, 1);
    this.setState({ listViewData: newData });
    */
}
/*-------------------------------------------------*/

